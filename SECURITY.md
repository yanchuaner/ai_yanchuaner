# 燕中 AI 安全策略

本文件定义 `ai_yanchuaner` 的漏洞披露与仓库级安全红线。跨项目身份、数据分级、计费和事件响应规则见工作区 `docs/security-and-privacy.md`。

## 私密披露

未修复漏洞细节不得创建公开 Issue。报告应通过 [GitHub 私密漏洞报告](https://github.com/yanchuaner/ai_yanchuaner/security/advisories/new) 提交；若仓库 Security 页面没有 **Report a vulnerability** 按钮，只创建标题为 `[安全联系请求]` 的 [无细节 Issue](https://github.com/yanchuaner/ai_yanchuaner/issues/new)，请维护者提供私密通道。完整报告包含受影响版本、入口、复现条件、影响范围和脱敏证据。不得提交真实 API Key、OIDC Secret、Cookie、grant、数据库备份、用户消息或知识原文。

维护者需要确认报告、完成分级、确定修复与凭据轮换范围，并在修复部署后协调披露。凭据一旦进入聊天、截图、日志或 Git 历史，应先撤销或轮换；只删除当前文件不能消除泄露。

## 信任边界

- 浏览器只持有加密、`HttpOnly`、`Secure` 的站点会话，不接触 YanCore grant、应用 Key 或供应商凭据。
- AI BFF 负责会话、CSRF、主体绑定和输入边界；模型调用只进入 `api.yanchuaner.cn` 统一网关。
- 主站是唯一身份源。AI 不提供本地密码注册，不复制主站用户表，不自行提升角色。
- 会话、角色、世界、知识、记忆、媒体元数据和用户设置按主体隔离；服务端不得信任客户端传入的所有者 ID。
- Open WebUI 与 LiteLLM 是内网过渡/设施组件，不定义公网产品权限，也不得访问个人公益账本以外的隐式权限。

## 数据与凭据

- 生产 Secret 只从受控环境或凭据服务注入，不进入仓库、镜像、日志、追踪、备份清单或客户端响应。
- 用户自配媒体凭据必须强加密、按主体隔离、只在对应适配器调用时解密；管理员界面只显示脱敏值。
- 默认日志不得记录消息正文、提示词、知识片段、上传内容、Cookie、Authorization、grant、Key、邮箱或手机号。
- 文件和向量索引不是跨服务接口；删除用户数据时同时处理权威数据、派生索引、缓存和受控备份保留期。
- 导入角色卡、知识文件和媒体前校验大小、类型、编码和解压边界；导出内容不得混入其他主体数据或服务端凭据。

## 工作流与扩展

- 内置工作流也必须声明 scope、副作用、超时、重试和计费类别。
- 外部插件在 manifest、来源校验、权限授权、资源限制、网络策略、审计、停用和卸载门禁完成前不得执行。
- 工具调用、外部抓取和媒体服务按不可信输入处理；不得把模型输出直接作为 Shell、SQL、路径或权限决策。
- 付费能力只能通过统一网关调用。身份、账本或限流状态不确定时拒绝派发。

## 发布门禁

发布前至少验证：OIDC/PKCE 和回调白名单、Cookie 与 CSRF、普通主体越权、停用撤销、受限 Key、流式取消、文件导入边界、敏感信息扫描、备份恢复及依赖/镜像来源。任何关键门禁缺失时，只能记录为未验收，不得描述为生产安全。
